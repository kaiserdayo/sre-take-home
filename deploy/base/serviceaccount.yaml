apiVersion: v1
kind: ServiceAccount
metadata:
  name: candidate-api
  labels:
    app.kubernetes.io/name: candidate-api
  annotations:
    # For AKS Workload Identity, uncomment and set the client ID:
    # azure.workload.identity/client-id: "<MANAGED_IDENTITY_CLIENT_ID>"
